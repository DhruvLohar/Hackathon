const styles = {
    flexCenter: "flex justify-center items-center",
    flexBetween: "flex justify-between items-center"
};

export const layout = {
    sectionColumn: `h-screen w-full sm:px-3 px-5 flex-col ${styles.flexCenter}`,
    sectionRow: `h-screen w-full sm:px-3 px-5 ${styles.flexCenter}`
};

export default styles;