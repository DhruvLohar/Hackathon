const styles = {
    flexCenter: "flex justify-center items-center"
};

export const layout = {
    sectionColumn: `absolute h-screen w-full sm:px-3 px-5 flex-col ${styles.flexCenter}`,
    sectionRow: `absolute h-screen w-full sm:px-3 px-5 ${styles.flexCenter}`
};

export default styles;